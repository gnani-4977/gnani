def ms(arr):
    result=0
    for i in range(len(arr)):
        sum=0
        for j in range(i,len(arr)):
            sum=sum+arr[j]
            result=max(result,sum)
    return result
arr=[2,3,-8,7,-1,2,3]
print(ms(arr))
